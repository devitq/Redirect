# Redirect

A simple HTML page for internal needs. Parses query parameters and according to them shows certain content and redirects to another resource.

Query parameters:

- `redirect_to`

  required
- `message`

  default: `Redirecting you to the destination...`
- `time_to_redirect`

  default: `5`
